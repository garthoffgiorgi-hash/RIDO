// Placeholder — about/mission page. Scaffolding only; real content is a Design→Code handoff.
// Copy source: docs/business/overview.md + brand/brand-guide.md's message hierarchy (mission
// is the soul, surfaced here — never the marketing hero).
export default function AboutPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <h1 className="font-sora text-4xl font-bold text-midnight">Our promise</h1>
    </main>
  );
}
